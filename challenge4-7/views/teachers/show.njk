{% extends "layout.njk" %}

{% block content %}
    <div class="card show">
        <section class="avatar" style ="background: url({{ teacher.avatar_url }}) no-repeat center center / cover"></section>
        <section class="details">
            <h3>Detalhes</h3>

            <div class="item">
                <div>Nome</div>
                <div>{{ teacher.name }}</div>
            </div>

            <div class="item">
                <div>Idade</div>
                <div>{{ teacher.age }}</div>
            </div>

            <div class="item">
                <div>Escolaridade</div>
                <div>{{ teacher.schooling }}</div>
            </div>

            <div class="item">
                <div>Modalidade de ensino</div>
                <div>{{ teacher.type_of_class }}</div>
            </div>

            <div class="item">
                <div>Acompanhamento</div>
                <div>
                    {% for service in teacher.services %}
                        <span class="service">{{ service }}</span>
                    {% endfor %}
                </div>
            </div>

            <div class="item">
                <div>Desde</div>
                <div>{{ teacher.created_at }}</div>
            </div>

            <div class="button_show_page">

                <a href="/teachers/{{ teacher.id }}/edit" class="button button_show_page">Editar</a>

                <form id="form-delete" method="POST" action="/teachers?_method=DELETE">
                    <input 
                        type="hidden"
                        name="id"
                        value="{{ teacher.id }}"
                    >
                    <button class="button_show_page" type="submit">Deletar</button>
                </form>
            </div>
        </section>
    </div>
{% endblock content %}